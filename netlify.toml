[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: every path serves index.html so client-side
# routes (/members, /committees/:slug, /ifmsa, ...) work on
# direct load and refresh instead of 404ing.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Security headers (mirrors public/_headers for Cloudflare; Netlify reads both
# but the file wins, so keep them aligned).
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "SAMEORIGIN"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=(), browsing-topics=()"
    Strict-Transport-Security = "max-age=31536000"
    Content-Security-Policy = "default-src 'self'; base-uri 'self'; object-src 'none'; frame-ancestors 'self'; img-src 'self' data: blob: https:; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; script-src 'self' 'unsafe-inline' https://va.vercel-scripts.com; connect-src 'self' https://script.google.com https://script.googleusercontent.com https://*.googleusercontent.com https://vitals.vercel-insights.com; frame-src https://calendar.google.com https://www.canva.com https://drive.google.com https://*.googleusercontent.com; upgrade-insecure-requests"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
